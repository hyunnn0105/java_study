package com.kh.hw.person.controller;

// hw.member 참고하면서 해보기
public class Personcontroller {
    /*
    private Student[] s;
    private Employee[] e;

    public Personcontroller(){
        s = new Student[3];
        e = new Employee[10];
    }
    
    // 수정! 아무값이나 넣어둠
    public int[] PersonCount(){
        int[] count = new int[2]; // 실제 저장된 학생의 수
        for (Student student : s) {
            if (e == null) break;
//            count++;
        }
        int ecount = 0;
        for (Employee employee : e) { // 실제 저장된 직원의 수
            if (e == null) break;
            ecount++;
        }
        // 학생 + 회사원 값 한번에 리턴

        // return new int[]{count, ecount};
    }

    public void insertStudent(String name, int age, double height, double weight, int grade, String major){
        int count = PersonCount().length;
        e[count] = new Employee(name, age, height,
        weight, grade,  major);

    }

    public Student[] printStudent(){
        return s;
    }
    public void insertEmployee(String name, int age, double height, double weight, int salary, String dept){
        // 빈곳이 있으면 저장하기
//        s[i] = new Employee(name, age, height,weight,salary ,dept );
    }
    public Employee[] printEmployee(){
        return e;
    }

*/
}
